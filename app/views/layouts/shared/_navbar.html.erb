<nav class="navbar navbar-expand-lg navbar-dark bg-dark" style="border-bottom: 1px solid #00000038;">
    <div class="container">
        <a class="navbar-brand" href="<%= root_path %>">
            <img src="https://cdn-icons-png.flaticon.com/512/3339/3339009.png" alt="" height="24">
            Baitube
        </a>
        <%
=begin%>
 <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
<%
=end%>
        <% if channel_signed_in? %>
            <%= link_to my_channel_path do %>
                <% if current_channel.avatar.attached?%>
                    <img src="<%= url_for(current_channel.avatar)%>" class="rounded mx-auto d-block" width="50" id="profile-avatar-config">
                <% else %>
                    <a href="<%= my_channel_path %>"><%= current_channel.name %></a>
                <% end %>
            <% end %>
        <% else %>
            <a class="btn btn-outline-primary" href="<%= new_channel_session_path %>">Entrar</a>
        <% end %>
    </div>
</nav>
<br>

<!--
FAZER DEPOIS

<form class="d-flex">
    <input class="form-control me-2" type="search" placeholder="Pesquisar" aria-label="Pesquisar">
    <button class="btn btn-outline-light" type="submit">Pesquisar</button>
</form>
<br>
-->
